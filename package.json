{
  "name": "agent-ui",
  "version": "1.0.0",
  "description": "Chat UI for Claude CLI agent",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.6.1"
  }
}
